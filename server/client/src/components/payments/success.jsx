import { Typography } from "@mui/material";

const Success = () => {
    return (
        <>
            <Typography>Payment is successfully done.</Typography>
        </>
    );
}

export default Success;